# 0. Import a simple function from a simple file

A Python script that imports the `add` function from another file, adds two numbers, and prints the result using string formatting.


# 1. My first toolbox!

A Python script that imports addition, subtraction, multiplication, and division functions from `calculator_1.py`, performs calculations using two variables, and displays the results.

